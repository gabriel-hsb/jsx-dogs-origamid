import { styled } from "styled-components";

export const PostForm = styled.form`
  min-height: 100dvh;

  input[type="file"] {
    margin: 2rem 0 1rem;
    display: block;
    color: var(--font-color-bg-light)
  }
`;
